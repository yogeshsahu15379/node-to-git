/**
 * The button that brings the user back to the top of the page when they click it.
 * @element aes-back-to-top-button
 * @slot defaultSlot
 */
export declare class AesBackToTopButton {
  private(e: MouseEvent): void;
  render(): any;
}
