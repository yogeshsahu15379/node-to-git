import { html } from 'lit-html'
import { Meta, Canvas, Story, Preview } from '@storybook/addon-docs/blocks'

import tailwindThemeConfig from '../../../src/resolved-tailwind-config'

export const argTypes = {
  signature: {
    table: {
      disable: true,
    },
  },
  neutrals: {
    table: {
      disable: true,
    },
  },
}

<Meta
  title="Protons/Colors"
  parameters={{
    viewMode: 'docs',
  }}
/>

# Signature colors

The bright, bold colors of AES.

<Story
  name="Signature"
  args={{
    signature: Object.keys(tailwindThemeConfig.colors.signature).map(
      v => `bg-signature-${v}`,
    ),
  }}
  argTypes={argTypes}
>
  {({ signature }) => html`
    <div class="grid grid-cols-4 mb-10">
      ${signature.map(
        v =>
          html`
            <div>
              <div class="inline-block w-full h-64 ${v}"></div>
              <code>.${v}</code>
            </div>
          `,
      )}
    </div>
  `}
</Story>

# Neutral colors

The muted supporting colors of AES.

<Story
  name="Neutrals"
  args={{
    neutrals: Object.keys(tailwindThemeConfig.colors.neutrals).map(
      v => `bg-neutrals-${v}`,
    ),
  }}
  argTypes={argTypes}
>
  {({ neutrals }) => html`
    <div class="grid grid-cols-4 mb-10">
      ${neutrals.map(
        v =>
          html`
            <div>
              <div class="inline-block w-full h-64 ${v}"></div>
              <code>.${v}</code>
            </div>
          `,
      )}
    </div>
  `}
</Story>
