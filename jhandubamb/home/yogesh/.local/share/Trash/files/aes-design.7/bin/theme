#!/usr/bin/env bash
## Compile the theme and run browser-sync to watch for changes.
##
## Usage: fin theme
#: exec_target = cli

source "$(dirname $0)/.helper"
cd ${PROJECT_ROOT}

cd ${DESIGN_PATH}
npm start
