apiVersion: v1
kind: Service
metadata:
  name: {{ include "design.fullname" . }}
  labels:
    {{- include "design.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "design.selectorLabels" . | nindent 4 }}
