import { html } from 'lit-html'
import { Meta, Canvas, Story, Preview } from '@storybook/addon-docs/blocks'
import { ifDefined } from 'lit-html/directives/if-defined'

import { IconType } from '../../utils/enums'

<Meta title="Atoms/Menu Button" component="aes-menu-button" />

# Menu buttons

The menu button hides the text at mobile to only show the icon. Full text is shown at desktop.

## Simple menu button

<Canvas>
  <Story name="Menu Button">
    {() => html`
      <aes-menu-button icon="${IconType.LOGIN}" url="http://google.com">
        My AES
      </aes-menu-button>
      <aes-menu-button icon="${IconType.GLOBE}" url="http://google.com">
        Global - EN
      </aes-menu-button>
      <aes-menu-button> Search </aes-menu-button>
    `}
  </Story>
</Canvas>
